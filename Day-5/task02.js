function multiply(n) {
    let result = "";
    for (let i = 1; i <= 10; i++) {
        result += `${n} x ${i} = ${n * i}\n`;
    }
    console.log(result);
}

multiply(3);


/*Generate a Pyramid Pattern using Nested Loop as it is shown below:


*/
